让重写四个函数，实现动态分布器。
根据书上572页开始的代码很好写出来。

根据书上的抄一抄测试的时候竟然说我的没有8字节对齐。我错误的认为mem_brk是指针的位置。
使用tracefile1 是80分，tracefile2 是94分，这都是根据书上抄的。让我来优化一下。

优化的方向是块的结构和分配。

在隐式空闲链表中每次分配块都是从heap——listp开始一个一个往后寻找，哪怕后面的块是alloc也要遍历用来到接下来的块中，我们用显示空闲链表，在空闲块中使用指针，指向空闲的块，这样寻块的时候我们只需要在空闲块中找就可以。

然后使用分离适配管理空闲链表。

优化任务：
1.显示空闲链表
2.分离适配

显式的空闲链表感觉只在find-fit中使用到了。